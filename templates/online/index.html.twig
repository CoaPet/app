{% extends 'base_user.html.twig' %}

{% block title %}Programmes en ligne{% endblock %}

{% block body %}
<div class="intro nav-font small-font-size text-center bg-white">
    <section>
    <p>nondum tubarum cessante clangore vel milite locatPost emensos insuperabilis expeditionis eventus languentibus partium animis, quas periculorum varietas fregerat et laborum, nondum tubarum cessante clangore vel milite locat</p>
    </section>
</div>
<section id="list_activities">
    {% for activity in activities %}
        <div class="card-coach-activity d-flex justify-content-between w-100 my-1 my-md-5 py-2 py-md-5">
            <div class="w-md-50 d-flex justify-content-around">
                <div class="overflow-hidden card-act-image mx-0 text-left mx-auto mx-md-0">
                    <img class="w-100" src="{{ vich_uploader_asset(activity, 'activityFile')|imagine_filter('sizeOne') }}"
                         alt="{{ activity.title }}">
                </div>
            </div>
            <div class="details d-flex flex-column text-light {% if loop.index is odd %}pl-md-5{% else %}pr-md-5{% endif %}">
                <div>
                    <div class="w-100 text-left">
                        <h2 class="mb-0 w-100 medium-font-size main-font-black"><span class="main-font-semi-bold xl-font-size"> </span>{{ activity.title|upper }}</h2>
                    </div>
                    <div class="nav-font small-font-size">
                        <p class="mb-1">{{ activity.description|raw }}</p>
                        {% if activity.intensity is defined %}
                            <p class="my-0 nav-font-semi-bold mb-1">Intensité :
                                {% for i in 1..activity.intensity %}
                                    <img class="mx-1 pb-2" src="{{ asset('build/fire.svg') }}" alt="fire"/>
                                {% endfor %}</p>
                        {% endif %}
                        {% if activity.more %}
                            <p class="my-0 mb-1 nav-font-semi-bold">Type d'effort : <span class="flashy">{{ activity.more ?? '' }}</span></p>
                        {% endif %}
                    </div>
                </div>
                    <div class="row mt-4">
                        <a href="{{ path('app_online_choice') }}" class="btn btn-light mr-4 ">Je choisis l'offre qui me correspond</a>
                    </div>
            </div>
        </div>
    {% endfor %}
</section>

    <div class="bg-white text-center py-4">
        <p class="py-5 mb-0 main-font-bold medium-font-size">LES ÉTAPES DES PROGRAMMES EN LIGNE</p>
    </div>
    <div class="w-100 bg-white py-3 mb-0">
    {% include 'time.html.twig' %}
    </div>
    <div class="py-5 br-light text-center bg-white">
        <a class="btn border-primary text-primary nav-font-semi-bold small-14-font-size"  href="{{ path('app_login') }}">S'INSCRIRE</a>
    </div>


    {% include 'map.html.twig' %}
{% endblock %}
